{
  "name": "hustledesk",
  "version": "1.0.0",
  "description": "African-first micro-business management platform",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "e2e": "turbo run e2e",
    "clean": "turbo run clean",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@turbo/gen": "^1.10.12",
    "prettier": "^3.0.0",
    "prettier-plugin-tailwindcss": "^0.5.4",
    "turbo": "^1.10.12"
  },
  "packageManager": "pnpm@8.6.10",
  "engines": {
    "node": ">=18"
  }
}
